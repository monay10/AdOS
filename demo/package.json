{
  "name": "ados-demo-environment",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Official AdOS advertising-agency demo environment (Vega Reklam Ajansı). Deterministic, isolated, repeatable. No production data, no external dependencies.",
  "scripts": {
    "setup": "node src/setup.mjs",
    "reset": "node src/reset.mjs",
    "validate": "node src/validate.mjs",
    "test": "node --test"
  }
}
