# Book G · Part 2 — Business Analytics — Validation

> **Source of truth:** [`../../PRODUCT_TRUTH.md`](../../PRODUCT_TRUTH.md). Governing document:
> [`../1-execution-analytics/ANALYTICS_CONSTITUTION.md`](../1-execution-analytics/ANALYTICS_CONSTITUTION.md).
>
> **Laws:** *Analytics never influences execution directly · analytics never mutates · every
> metric has provenance · dashboard ≠ decision · same data, different views · analytics is
> immutable · every dashboard is derived · time is first-class · every visualization has data ·
> observability before optimization.*

Validation of Part 2 — the strong ✅ baseline of the book: KPIs render live, provenance is already
true, and the one honest gap (live time-window selection) is named, not hidden. Result:
**✅ PASS**.

---

## 1. Scope validated

| # | Document | Purpose | Tier focus |
|---|---|---|---|
| G003 | [`BUSINESS_ANALYTICS.md`](BUSINESS_ANALYTICS.md) | Campaign / ROAS / CTR / ROI / executive / per-client analytics that render today | ✅ |
| G004 | [`METRIC_PROVENANCE.md`](METRIC_PROVENANCE.md) | Owns Every Metric Has Provenance · Analytics is Immutable · Time is First-Class | ✅ |

## 2. Checks

| Check | Result | Notes |
|---|---|---|
| Deterministic KPI engine | ✅ PASS | `computeKpis` ([kpi.ts:39](../../domains/analytics-engine/src/report/kpi.ts#L39)) is pure math over one explicit `CampaignMetrics`; six KPIs — CTR/CPC/CPA/CPL/ROAS/ROI — at [kpi.ts:43-48](../../domains/analytics-engine/src/report/kpi.ts#L43); no division by zero via `safeDiv`, no float drift on `Money` minor units. |
| KPIs render live | ✅ PASS | Reports generated by `generateReport` ([routes.ts:1016](../../apps/web/src/routes.ts#L1016)) over hand-entered metrics ([routes.ts:1042-1047](../../apps/web/src/routes.ts#L1042)); ROAS/ROI/CTR + summary rendered at `/analytics` ([routes.ts:625-645](../../apps/web/src/routes.ts#L625)). |
| Executive verdict renders | ✅ PASS | `exceeded \| on_track \| at_risk` on `DashboardContent` ([executive-report.ts:40](../../domains/executive-ai/src/dashboard/executive-report.ts#L40)); generated ([routes.ts:1055](../../apps/web/src/routes.ts#L1055)), rendered `/executive` ([routes.ts:707-728](../../apps/web/src/routes.ts#L707)). |
| Per-client rollup renders | ✅ PASS | `buildReportSnapshot` ([routes.ts:1436-1488](../../apps/web/src/routes.ts#L1436)); `avgRoas` recomputed from source ([routes.ts:1470](../../apps/web/src/routes.ts#L1470)), rendered `/reports` ([routes.ts:685-696](../../apps/web/src/routes.ts#L685)). |
| Every Metric Has Provenance (Law 2) | ✅ PASS | `computeKpis` takes explicit named inputs; `CampaignReport` retains `metrics` beside `kpis` ([campaign-report.ts:34-35](../../domains/analytics-engine/src/report/campaign-report.ts#L34)) — provenance is a field, not a log. No magic numbers. |
| Only the narrative is AI | ✅ PASS | "KPIs are pure math; only the narrative is AI-generated" ([service.ts:24](../../domains/analytics-engine/src/report/service.ts#L24)); narrative stamped with `AIProvenance` ([service.ts:82-84](../../domains/analytics-engine/src/report/service.ts#L82)); the numbers carry no model. |
| Analytics is Immutable (Law 5) | ✅ PASS | One-way derivation Events → Metrics → Reports; recompute-not-rewrite proven by pure `computeKpis`; read-only proof — the one execution-state write, `recordLearning` ([routes.ts:1092](../../apps/web/src/routes.ts#L1092)), sits **outside** the analytics path. |
| Same Data, Different Views (Law 4) | ✅ PASS | Campaign, executive, and client views are three summaries of one mission truth; the client `avgRoas` is the mean of the very campaign ROAS figures beneath it — consistent by construction. |
| Dashboard ≠ Decision (Law 3) | ✅ PASS | Views show ROAS/CTR/ROI and a verdict; `at_risk` is an observation for a human, never an instruction or an automatic action. |
| Time is First-Class (Law 7) | ❌ NOTED | Honest gap: live time-window selection (7d/30d/quarter/year/lifetime) is **❌ ROADMAP** and cites no code; today's views are per-campaign/per-mission/per-client **snapshots**, not time-bucketed series. Both docs state this plainly; the pure KPI engine is ready to sit under a window when one is built. |
| Read-only hygiene (Law 1) | ✅ PASS | Analytics paths are pure reads (`.list`/`.get`); no write to mission, evidence, memory, or creative. Generating a report artifact is not mutating execution state. |
| Boundaries | ✅ PASS | 100% local, own-data-only, no vendor telemetry; hand-entered metrics, no external feed; AI narrative runs through the local AI Manager, leaving only a provenance stamp. |
| Invariant sentence | ✅ PASS | "Observability reveals reality; it never changes reality." present verbatim in both docs. |
| Citation accuracy / cross-refs | ✅ PASS | All cited `path:line` references exist; cross-doc and governing-doc links resolve. |
| Documentation-only hygiene | ✅ PASS | Only `book-g/2-business-analytics/` files added. |
| Forbidden legacy label | ✅ PASS | Absent. |

## 3. Verdict

**✅ PASS.** Part 2 is the book's strong baseline: the KPI engine, the campaign report, and the
three live views render today; provenance and immutability are already true in shipped code, proven
structurally rather than asserted. The single gap — live time-window selection — is tiered **❌**
and named openly in both documents, which is exactly what keeps the ✅ tier honest.

---

*Documentation only. No application code, packages, domains, or tests were modified.
Aligned to PRODUCT_TRUTH.md.*
